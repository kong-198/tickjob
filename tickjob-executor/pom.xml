<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.kong</groupId>
        <artifactId>tickjob</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>tickjob-executor</artifactId>
    <name>tickjob-executor</name>
    <description>执行器 SDK：业务方引入即可注册到调度中心并按 @JobHandler 执行任务</description>

    <dependencies>
        <dependency>
            <groupId>com.kong</groupId>
            <artifactId>tickjob-common</artifactId>
        </dependency>

        <!-- 以 Spring Boot Starter 形式交付：只依赖 starter 基础能力，不强制宿主是 Web 应用 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
